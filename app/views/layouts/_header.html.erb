<header>
  <nav class="navbar navbar-default " role="navigation">
	<div class = "container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
	<span class="sr-only">Toggle navigation</span>
	<span class="icon-bar"></span>
	<span class="icon-bar"></span>
	<span class="icon-bar"></span>
      </button>
      <%=link_to "yyAPP" ,root_path , class:"navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
	<li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">软件分类<b class="caret"></b></a>
          <ul class="dropdown-menu">
	    <li><%= link_to "全部软件",tag_path("软件") %></li>
	    <% soft_list.each do |tag| %>
	      <li><%= link_to tag,tag_path(tag) %></li>
	    <% end %>
          </ul>
	</li>
	<li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">游戏分类<b class="caret"></b></a>
          <ul class="dropdown-menu">
	    <li><%= link_to "全部游戏",tag_path("游戏") %></li>
	    <% game_list.each do |tag| %>
	      <li><%= link_to tag,tag_path(tag) %></li>
	    <% end %>
          </ul>
	</li>
      </ul>
      <form class="navbar-form navbar-left" role="search" action="/search" method="get">
	<div class="form-group searchbox">
          <input type="text" class="form-control" placeholder="搜APP" name="q">
	</div>
	<button type="submit" class="btn btn-default">So</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
	 <% if can? :manage, User %>
		<li><%= link_to "管理用户", users_path %></li> 
		<li><%= link_to "管理APP", apps_path %></li> 
	 <% end %>
	<% if user_signed_in?  %>
	  <li class="dropdown">
	    <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= gravatar_for current_user, size: 22 %> <%= current_user.username %></a> 
            <ul class="dropdown-menu">	     
	      <li><%= link_to "资料", current_user %></li>
              <li><%= link_to "修改资料", edit_user_registration_path %> </li>
              <li class="divider"></li>
              <li> <%= link_to "注销", destroy_user_session_path, method: "delete" %></li> 
            </ul>
	  </li>
        <% else %>
	       <li><%= link_to "登录", new_user_session_path %></li>
          <li><%= link_to "注册", new_user_registration_path %></li>
        <% end %>     
      </ul>
    </div><!-- /.navbar-collapse -->
	</div>
  </nav>
</header>
